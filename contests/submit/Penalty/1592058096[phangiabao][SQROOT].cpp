#include <bits/stdc++.h>
#define fi freopen("SQROOT.inp", "r", stdin)
#define fo freopen("SQROOT.out", "w", stdout)
using namespace std;

int main()
{
    fi;fo;
    unsigned long long n;
    cin >> n;
    cout << (int)sqrt(n);
}
